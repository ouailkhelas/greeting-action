name: "Custom Greeting Action"
description: "Greets someone in different languages"

inputs:
  name:
    description: "Person's name to greet"
    required: true
  language:
    description: "Language code (fr/en/ar)"
    required: false
    default: "en"

runs:
  using: "composite"
  steps:
    - name: Display greeting
      shell: bash
      run: |
        # Check if name is provided
        if [ "${{ inputs.name }}" == "" ]; then
          echo "❌ Error: Name is required"
          exit 1
        fi
        
        # Display greeting based on language
        case "${{ inputs.language }}" in
          "fr")
            echo "🇫🇷 Bonjour ${{ inputs.name }} !"
            ;;
          "en")
            echo "🇬🇧 Hello ${{ inputs.name }}!"
            ;;
          "ar")
            echo "🇩🇿 مرحبا ${{ inputs.name }} !"
            ;;
          *)
            echo "👋 Hi ${{ inputs.name }}!"
            ;;
        esac
